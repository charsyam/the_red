import logging
import json_logging


def init_log(app, filepath):
#    json_logging.init_fastapi(enable_json=True)
#    json_logging.init_request_instrument(app)
#    logger = json_logging.get_request_logger()
#    logger.addHandler(logging.handlers.TimedRotatingFileHandler(filepath, when='h'))
#    json_logging.init_request_instrument(app)

#   return logger
    pass
